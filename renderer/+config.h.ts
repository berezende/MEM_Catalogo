// Configs
export default {
    // Use Client Routing (React Router) + Server Routing (Vike)
    // This means Vike will render the initial HTML, then React Router takes over
    passToClient: ['pageProps']
}
